# Campaign images

Add optimized promotional images to this folder, then list their public paths in `public/site-config.js` under `campaignImages`. After a successful WL submission, the interface selects one configured image at random.

WebP is recommended for photographic or illustrated campaign art. Keep each file reasonably sized for fast mobile loading.
