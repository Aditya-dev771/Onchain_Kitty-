{
  "name": "onchain-kitty-maximalist-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Onchain Kitty maximalist WL application and ecosystem website",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check api/wl.js && node --check api/health.js && node --check api/admin/export.js && node --test test/core.test.mjs && node test/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
