# WL data

The server creates `wl-submissions.json` here after the first successful application. That generated file is intentionally ignored by version control and must never be moved into the public directory.
